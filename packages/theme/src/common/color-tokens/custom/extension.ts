import { localize } from '@ali/ide-core-common';
import { registerColor } from '../../color-registry';

export const ktStatusBarExtensionDebugginBackground = registerColor('kt.statusBar.extensionDebuggingBackground', { dark: '#FF8F5C', 'light': '#FF8F5C', hc: '#FF8F5C' }, localize('Background of StatusBar while extension is debugging.'));
