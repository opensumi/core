import { ExpressFileServerModule } from '../../src/node';

describe('template test', () => {
  it('ExpressFileServerModule', () => {
    // const cls = new ExpressFileServerModule();
    // expect(cls.providers).toEqual([]);
  });
});
