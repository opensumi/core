providers = ["..."]

[variables]
NODE_ENV = 'production'

[phases.setup]
nixPkgs = ["...", "libsecret"]
aptPkgs = ["...", "build-essential", "libsecret-1-dev"]

[phases.build]
cmds = ['yarn build:all']

[start]
cmd = 'yarn start:prod'
