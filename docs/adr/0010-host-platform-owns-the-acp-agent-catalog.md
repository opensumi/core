---
status: proposed
---

# The host platform owns the ACP Agent Catalog

Only ACP Agents registered and validated by the Web IDE host platform will be available for Task Launch. Developers may select an Agent and save project defaults, but B-lite will not accept arbitrary ACP endpoints or server commands. This keeps process execution, credentials, tenancy, and protocol compatibility within the host platform's security boundary.
