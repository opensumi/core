---
status: accepted
---

# Keep one cross-Agent Task List

Agentic Layout presents one persistent Agent Task List containing retained Tasks from every ACP Agent, grouped by Project rather than partitioned by the Agent currently selected for Task Launch. Each Task carries its immutable originating Agent identity for routing and presentation; changing launch defaults must not hide existing work, while an explicit Agent filter may narrow the unified list without changing its ownership model.
